# Маппинг переименования колонок для каждого источника
# Формат: исходное_название: новое_название

oed:
  # Пример: "Дата": "date"

okk:
  # Пример: "Оценка": "score"

learning:
  # Пример: "ФИО": "employee_name"

kpi:
  # Пример: "Показатель": "metric_name"

users:
  # Пример: "Табельный номер": "employee_id"

enps:
  # Пример: "Балл": "score"

attestations:
  # Пример: "Результат": "result"
